package com.lssgoo.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors
val Primary = Color(0xFF9ED7FF) // The new primary color
val PrimaryDark = Color(0xFF6BB9E8)
val PrimaryLight = Color(0xFFC7EEFF)

// Secondary Colors (mapping to black as per request)
val Secondary = Color(0xFF000000) // Black
val SecondaryDark = Color(0xFF212121) // A very dark gray for distinction
val SecondaryLight = Color(0xFF424242) // A dark gray

// Background Colors
val Background = Color(0xFFFFFFFF) // White
val Surface = Color(0xFFFFFFFF) // White
val SurfaceVariant = Color(0xFFF0F0F0) // Light gray to distinguish from pure white

// Text Colors
val TextPrimary = Color(0xFF000000) // Black
val TextSecondary = Color(0xFF333333) // Dark gray
val TextTertiary = Color(0xFF666666) // Medium gray

// Accent Colors - keeping original values for now, can be adjusted if needed
val Accent = Color(0xFF4CAF50)
val AccentYellow = Color(0xFFFFC107)
val AccentRed = Color(0xFFF44336)

// Status Colors - keeping original values for now, can be adjusted if needed
val Success = Color(0xFF10B981)
val Warning = Color(0xFFF59E0B)
val Error = Color(0xFFEF4444)
val Info = Color(0xFF3B82F6)

// Gradient Colors - adjusting to the new primary scheme
val GradientStart = Color(0xFF9ED7FF)
val GradientEnd = Color(0xFF7CB8E0)
